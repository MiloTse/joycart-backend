<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOGS" value="./logs" />
    <property name="APP_NAME" value="joycart-backend" />

    <!-- 控制台输出格式 -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %X{requestId:--} %X{userId:--} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件日志输出 -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/${APP_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %X{requestId:--} %X{userId:--} %X{requestUri:--} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 按天滚动日志文件 -->
            <fileNamePattern>${LOGS}/archived/${APP_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 保留30天的历史记录 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- ERROR级别日志单独记录 -->
    <appender name="ErrorFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/${APP_NAME}-error.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %X{requestId:--} %X{userId:--} %X{requestUri:--} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/${APP_NAME}-error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- 不同环境的配置 -->
    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="Console" />
            <appender-ref ref="RollingFile" />
            <appender-ref ref="ErrorFile" />
        </root>
        <!-- 应用包的日志级别 -->
        <logger name="com.joycart.backend" level="debug" />
        <!-- 显示SQL -->
        <logger name="org.hibernate.SQL" level="debug" />
    </springProfile>

    <springProfile name="prod">
        <root level="info">
            <appender-ref ref="RollingFile" />
            <appender-ref ref="ErrorFile" />
            <appender-ref ref="Console" />
        </root>
        <!-- 应用包的日志级别 -->
        <logger name="com.joycart.backend" level="info" />
    </springProfile>

    <!-- 默认配置 -->
    <root level="info">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
        <appender-ref ref="ErrorFile" />
    </root>
</configuration> 